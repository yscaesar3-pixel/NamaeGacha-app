{
  "name": "namaegacha",
  "version": "1.0.0",
  "private": true,
  "description": "なまえガチャ - ランダム姓名メーカー（iPhone / Capacitor）",
  "scripts": {
    "validate-data": "node tools/validate_name_data.js",
    "validate-data:strict": "node tools/validate_name_data.js --strict-counts",
    "test:generator": "node tools/test_generator.js",
    "test:priority-surnames": "node tools/test_priority_surnames.js",
    "cap:add-ios": "npx cap add ios --packagemanager SPM",
    "cap:sync": "npx cap sync ios",
    "cap:open": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^8.5.1",
    "@capacitor/ios": "^8.5.1",
    "@capacitor-community/admob": "^8.1.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.1"
  }
}
